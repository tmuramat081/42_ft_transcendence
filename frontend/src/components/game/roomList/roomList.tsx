import { User } from '@/types/user';
import { useEffect } from 'react';

type Props = {
  user: User
};

export default function RoomList({ user }: Props) {
  useEffect(() => {
    

  }, []);
  return (

  )

}
